<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jda8301.spellarhyme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen.ScreenColor">
        <activity
            android:name=".WelcomeActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".JoinClassActivity" />
        <activity android:name=".TeacherLoginActivity" />
        <activity android:name=".PlayerLoginActivity" />
        <activity android:name=".CreateTeacherAccountActivity" />
        <activity android:name=".ThreeWordsAtATime" />
        <activity android:name=".OneWordAtATimeListActivity" />
        <activity android:name=".LevelsActivity" />
        <activity android:name=".OneWordAtATimeGameActivity" />
        <activity android:name=".TeacherHomeActivity"></activity>

    </application>

</manifest>